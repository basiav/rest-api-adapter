const URL_BASE = 'https://jsonplaceholder.typicode.com/';
const COMMENTS_LIMIT = 5;

const config = {
    headers: {"Accept-Encoding": "gzip,deflate,compress"}
};

module.exports = { URL_BASE, COMMENTS_LIMIT, config };